services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai_parser_backend
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    networks:
      - parser_net

networks:
  parser_net:
    driver: bridge